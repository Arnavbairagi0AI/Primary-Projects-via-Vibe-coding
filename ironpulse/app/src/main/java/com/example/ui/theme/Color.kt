package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Athletic Gym Brand Colors (IronPulse - Geometric Balance Theme)
val GymRed = Color(0xFFDC2626)      // Vibrant athletic red (red-600)
val GymRedDark = Color(0xFF991B1B)  // Deeper crimson red for accents (red-800)
val GymRedLight = Color(0xFFEF4444) // Highly saturated neon red highlights (red-500)
val GymBlack = Color(0xFF0A0A0A)     // Absolute matte deep black background
val GymSlate = Color(0xFF161616)     // Premium dark grey card background (#161616)
val GymSlateLight = Color(0xFF1A1A1A) // Secondary dark surface / border block (#1A1A1A)
val GymBorder = Color(0x0DFFFFFF)    // Subtle card border (white.copy(alpha = 0.05f))
val GymBorderBright = Color(0x1AFFFFFF) // Slightly brighter border (white.copy(alpha = 0.10f))
val TextWhite = Color(0xFFF1F5F9)    // Clean white text (slate-100)
val TextGray = Color(0xFF94A3B8)     // Readability-focused body gray (slate-400)
val TextGrayMuted = Color(0xFF64748B) // Muted descriptors (slate-500)
val BmiEmerald = Color(0xFF22C55E)   // Normal range green (emerald-500)
val BmiAmber = Color(0xFFF59E0B)     // Over/Under range amber
